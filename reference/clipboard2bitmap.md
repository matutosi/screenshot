# Save clipboard image to temporary BMP file

This function works only on windows. This function saves the image
currently in the clipboard to a temporary BMP file.

## Usage

``` r
clipboard2bitmap()
```

## Value

Path to the temporary BMP file.

## Examples

``` r
if (FALSE) { # \dontrun{
clipboard2bitmap()
} # }
```
