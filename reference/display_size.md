# Get the size of the display.

This function works only on windows.

## Usage

``` r
display_size()
```

## Value

A list with two elements, `width` and `height`, which are the width and
height of the display.

## Examples

``` r
if (FALSE) { # \dontrun{
display_size()
} # }
```
